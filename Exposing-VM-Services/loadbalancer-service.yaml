apiVersion: v1
kind: Service
metadata:
  name: web-server-loadbalancer
  namespace: default
  labels:
    app: web-server
spec:
  type: LoadBalancer
  selector:
    kubevirt.io/vm: web-server-vm
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
